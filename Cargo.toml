[package]
name = "shaw-opencode-stats"
description = "A maintained fork of OpenCode Stats, a terminal dashboard for OpenCode usage statistics"
version = "1.4.2-shawlaw.1"
edition = "2024"
authors = ["Shawlaw <shawlaw@126.com>"]
license = "MIT"
repository = "https://github.com/Shawlaw/opencode-stats"
publish = false
categories = ["command-line-utilities", "visualization"]
keywords = ["opencode", "cli", "tui", "dashboard", "statistics"]
readme = "README.md"

[[bin]]
name = "shaw-oc-stats"
path = "src/main.rs"

[dependencies]
ab_glyph = "0.2.32"
arboard = "3.6.1"
chrono = { version = "0.4.44", features = ["serde"] }
clap = { version = "4.6.1", features = ["derive"] }
crossterm = { version = "0.29.0", features = ["event-stream"] }
dirs = "6.0.0"
image = "0.25.10"
imageproc = "0.26.2"
tiny-skia = "0.12.0"
json5 = "1.3.1"
ratatui = "0.30.0"
reqwest = { version = "0.13.4", features = ["json"] }
rusqlite = { version = "0.40.0", features = ["bundled"] }
rust_decimal = { version = "1.42.0", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
toml = "1.1.2"
thiserror = "2.0.18"
tokio = { version = "1.52.3", features = [
    "sync",
    "rt-multi-thread",
    "macros",
    "time",
] }
colored = "3.1.1"
terminal-light = "1.8.0"
itertools = "0.14.0"
nucleo-matcher = "0.3"
unicode-width = "0.2.2"
futures = "0.3.32"
clap_complete = "4.6.5"
color-eyre = "0.6.5"

[profile.release]
codegen-units = 1 # Allows compiler to perform better optimization.
lto = true        # Enables Link-time Optimization.
opt-level = "s"   # Prioritizes small binary size. Use `3` if you prefer speed.
strip = true      # Ensures debug symbols are removed.
